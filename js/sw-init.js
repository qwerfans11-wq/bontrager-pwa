// js/sw-init.js — kept for backward compatibility after file split.
// Service worker registration is handled in js/core.js (_pwaRegisterSW).
(function(){
  if(typeof window === 'undefined') return;
})();
